<!doctype html>
<html lang="en">
  <head>
      {% include "head.njk" %}

  </head>
  <body>
    <header>
      {% include "header.njk" %}
    </header>

    <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>

      {{ content | safe }}
    </main>

    <footer>
      {% include "footer.njk" %}
    </footer>

    <!-- Current page: {{ page.url | url }} -->
  </body>
</html>
